case "${OSTYPE}" in
# MacOSX
darwin*)
    # ここに設定
    [ -f ${ZSHRC_DIR}.zshrc.osx ] && source ~/dotfiles/.zshrc.d/.zshrc.osx
    ;;
# Linux
linux*)
    # ここに設定
    [ -f ${ZSHRC_DIR}.zshrc.linux ] && source ~/dotfiles/.zshrc.d/.zshrc.linux
    ;;
esac
